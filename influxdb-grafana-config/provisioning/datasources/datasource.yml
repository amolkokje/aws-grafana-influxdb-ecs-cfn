apiVersion: 1

datasources:
- name: influxdb_sample_datasource
  type: influxdb
  access: proxy
  url: ${INFLUXDB_URL}
  password: ${INFLUXDB_PASSWORD}
  user: ${INFLUXDB_USER}
  database: ${INFLUXDB_DB}
  isDefault: true
  version: 1
  editable: true